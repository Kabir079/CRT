#include <stdio.h>

// variable declaration

#define LENGHT 10
#define WIDTH 5
#define newline '\n'

int main() 
{
    
    int area;
     
    area = LENGHT * WIDTH;
    printf("value of area : %d", area);
    printf("%c, newline");
     
     return 0;
}
