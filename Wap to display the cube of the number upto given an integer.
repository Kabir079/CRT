#include <stdio.h>
void main()
 {
    int i,d;
    printf("Input number of terms : ");
    scanf("%d", &d);
    for(i=1;i<=d;i++)
    {
	 printf("Number is : %d and cube of the %d is : %d \n",i,i, (i*i*i));     
    }
 }
