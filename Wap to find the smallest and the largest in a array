#include <stdio.h>

int main()
{
    int array[10],min,max,i;
    for(i=0;i<10;i++)
    {
        scanf("%d",&array[i]);
    }
    min = a[0];
    max = a[0];
    for(i=1;i<10;i++)
    {
        if(a[i]<min)
        {
            min = a[i]
        }
        if(a[i]>max)
        {
            max = a[i]
        }
    }
    printf("\n Smallest elements of array : %d",min);
    printf("\n Largest elements of array : %d",max);
    }
    return 0;
}
